import React from 'react'
import './Header.css'

export default function Header() {
  return (
    <header className="site-header" aria-label="Site header">
      {/* intentionally minimal — title is in the curved SVG on Hero */}
    </header>
  )
}
